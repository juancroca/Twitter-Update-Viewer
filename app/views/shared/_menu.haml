%ul.menu
  - if user_signed_in?
    %li
      = link_to "#{ current_user.twitter_ids.empty? ? "Add" : "Edit" } Twitter Id", new_twitter_ids_path
    %li
      |
      = link_to "View Twitter Updates", twitter_ids_path
    %li
      |
      = link_to('Logout', destroy_user_session_path, :method => :delete)
  - else
    %li
      = link_to('Login', new_user_session_path)